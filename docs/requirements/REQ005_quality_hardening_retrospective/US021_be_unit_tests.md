# US021 — Backend unit & integration test specification (stub)

**No BE changes.** US021 moves `@testing-library/dom` from `dependencies` to `devDependencies` in `web/package.json`. This is a frontend-only package hygiene story — no Go files, no Go services, no API contracts are touched.

**BE Dev: there is nothing to implement or test for US021. This stub exists so the coverage matrix is complete.**
