# US015/be_makefile_consolidation_and_retire_startup_scripts

**Requirement:** REQ006
**Story:** US015
**Track:** BE
**Service:** services/agent-board   (nominal — repo-root + Makefile + docs + agent-defs sweep; NO Go production code touched — see Notes)
**Status:** in_progress
**Blocked by:** none (soft sequencing — see Notes; US015 SHOULD ship before or with US010 per architecture D-013 / D-014 / R-6, but no hard `Blocked by`)
**Worked-by:** be-dev-2026-06-06T00-00-00Z-a3f7
**Implements:** REQ006/US015 AC (all scenarios — delete `startup.sh` / `shutdown.sh`, four new Makefile `dev-*` targets, `PG_CONN := → ?=` flip [absorbed from former US011 per D-014], new `DEV_PG_CONN ?=`, PID + log file conventions byte-identical to `startup.sh`, e2e family byte-identical, zero `DB_URL` in any new recipe, doc + agent-def sweep, tech-debt strike-throughs), architecture §3 US015 touch row (full table), architecture §11 D-013 (US015 charter) + D-014 (US011 absorbed, Option A union), architecture §10.1 (live-e2e + 3-clean-run flake check required).

## Goal
Retire `startup.sh` / `shutdown.sh` at repo root and consolidate the local-dev workflow into the Makefile as a new `dev-*` family (`dev-up`, `dev-down`, `dev-migrate`, `dev-seed`). Add `DEV_PG_CONN ?=` for the dev family on port 5432. Flip the existing `PG_CONN := → ?=` for the e2e family on port 15432 (absorbed from former US011 per D-014, Option A union — BOTH `?=` overrides preserved). Sweep all `startup.sh` / `shutdown.sh` references across `README.md`, `tests/e2e/README.md`, `services/agent-board/README.md` (if exists), `.claude/agents/*.md`, `CLAUDE.md`. Strike-through `docs/tech_debt.md` line 86 and any startup/shutdown reference.

## Scope
- **In:** DELETE `startup.sh` at repo root (~38 lines). DELETE `shutdown.sh` at repo root (~30 lines).
- **In:** Edit `Makefile` line ~16: change `PG_CONN := postgres://...:15432/...` → `PG_CONN ?= postgres://...:15432/...` (default URL byte-identical; one-character operator change — absorbed from former US011 per D-014). Add `DEV_PG_CONN ?= postgres://agent_board:agent_board@localhost:5432/agent_board?sslmode=disable` on its own line. Add four new targets: `dev-up`, `dev-down`, `dev-migrate`, `dev-seed`. PID files (`.mcp.pid`, `.api.pid`, `.web.pid`) and log files (`mcp-server.log`, `api-server.log`, `web.log`) at repo root, mirroring `startup.sh` byte-for-byte (architecture §3 US015 + D-013 Q3). Update `make help` (if present) to list the four new targets.
- **In (sweep):** Edit `README.md` (repo root) to replace any `./startup.sh` / `./shutdown.sh` references with `make dev-up` / `make dev-down` and add a brief native-Postgres prerequisite paragraph (D-013 Q1). Edit `tests/e2e/README.md` to add 1–2 lines distinguishing `dev-*` (native, `:5432`) from `e2e-*` (compose, `:15432`). Edit `services/agent-board/README.md` IF it exists and references the scripts. Run `git grep -l 'startup\.sh\|shutdown\.sh' .claude/agents/` and replace references with `make dev-*` equivalents in each match. Run `python3 scripts/sync-gemini.py` AFTER agent-file edits per project rule (CLAUDE.md §"Editing agent definitions"). Edit `CLAUDE.md` if it references the scripts (likely no-op).
- **In:** Strike-through `docs/tech_debt.md` line 86 with `→ fixed in REQ006/US015 (absorbed from former US011 for line 86; PG_CONN now ?= and env-overridable)`. Strike-through any other `tech_debt.md` line that references `startup.sh` / `shutdown.sh` directly with `→ fixed in REQ006/US015`.
- **Out:** ANY change to the existing `e2e-*` recipe bodies — they MUST be byte-identical per US015 AC scenario "existing `make e2e-*` targets are byte-identical". Variable additions / reorderings above the recipes that do not change resolved values are acceptable (the `PG_CONN := → ?=` flip falls in this category — the default value is byte-identical).
- **Out:** Any spot-fix to `startup.sh` env-var name — the file is DELETED, not edited (architecture D-013 alternative (a) explicitly rejected this).
- **Out:** A `dev-up-docker` companion target (architecture D-013 alternative (b) rejected — `make e2e-up` already provides the dockerised path).
- **Out:** Renaming `e2e-*` targets to `docker-*` (D-013 alternative (c) rejected).
- **Out:** Introducing a process supervisor (`tmux`/`foreman`/`overmind`/`concurrently`/`pm2`) — D-013 alternative (e) rejected.
- **Out:** Adding the `scripts/review/test/test_makefile_pg_conn.sh` regression test that original US011 had proposed — explicitly out of scope per D-014.
- **Out:** Hand-editing `.gemini/agents/*.md` directly — those are regenerated by `python3 scripts/sync-gemini.py` (CLAUDE.md rule). Only edit `.claude/agents/*.md`.
- **Out:** Any production Go code change. Any change inside `services/agent-board/internal/`, `services/agent-board/cmd/`. Any test file change inside `services/agent-board/` or `web/`. Any `docker-compose.yml` edit (that's US010's lane).

## Files touched (estimated, exclusive)
- `startup.sh` (DELETE)
- `shutdown.sh` (DELETE)
- `Makefile` (edit — flip line ~16 `:=` → `?=`; add `DEV_PG_CONN ?=`; add four `dev-*` targets; optional `make help` update)
- `README.md` (edit — sweep `startup.sh` / `shutdown.sh` references, add dev workflow paragraph)
- `tests/e2e/README.md` (edit — add `dev-*` vs `e2e-*` distinguisher; sweep references)
- `services/agent-board/README.md` (edit — CONDITIONAL on file existing AND referencing the scripts)
- `.claude/agents/*.md` (edit — every file in `git grep -l 'startup\.sh\|shutdown\.sh' .claude/agents/`)
- `.gemini/agents/*.md` (regenerated by `scripts/sync-gemini.py` — do NOT hand-edit; the regenerator owns the diff)
- `CLAUDE.md` (edit — CONDITIONAL on `git grep -n 'startup\.sh\|shutdown\.sh' CLAUDE.md` returning hits; likely no-op)
- `docs/tech_debt.md` (edit — strike-through line 86 + any startup/shutdown references)

(Total: ~7–10 files depending on conditional sweeps. **Scaffold-task note:** `Makefile` is a single-writer file with consolidation-scope edits. No other REQ006 task touches `Makefile` — US010 leaves it alone per architecture §3 — so no `Blocked by` chain is needed within REQ006. Nonetheless this task is a meaningful change to a shared file; if any future REQ006 follow-up needs to touch the Makefile, that follow-up should `Blocked by:` US015.)

## Test contract
US015 has no in-Go / in-React unit tests. The "tests" are behaviour assertions:
- `git ls-files startup.sh shutdown.sh` returns empty (files deleted).
- `git grep -nE 'startup\.sh|shutdown\.sh'` returns zero hits outside REQ006 docs (this story file, the sign-off log, the architecture file's historical references in §3 and the decision log).
- `make e2e-up && make e2e-seed && make e2e-run && make e2e-down` continues to pass green (the `e2e-*` family is byte-identical — architecture §3 US015 + D-013 explicit).
- `make dev-up` starts three backgrounded native processes; PID files appear at `.mcp.pid` / `.api.pid` / `.web.pid`; log files appear at `mcp-server.log` / `api-server.log` / `web.log`. `make dev-down` removes them and is idempotent on re-invocation.
- `make dev-migrate` applies all migrations against `$(DEV_PG_CONN)`. `make dev-seed` applies fixtures from `tests/e2e/data/seeds/` against `$(DEV_PG_CONN)`.
- `git grep -nE 'DB_URL' Makefile` returns zero hits in any new `dev-*` recipe (architecture §3 US015 + AC scenario "DATABASE_URL is the only DB env var").
- `PG_CONN=postgres://... make e2e-seed` and `DEV_PG_CONN=postgres://... make dev-migrate` both honor the env override.
- `make dev-down` exits 0 on a second invocation when nothing is running (idempotency per AC).

Tester's `US015_be_unit_tests.md` will contain UT-* IDs framed as these behavioural assertions plus zero in-Go unit tests.

## Implementation notes
- **Process lifecycle (architecture D-013 Q3 — verbatim from `startup.sh`):**
  - `make dev-up`: launch `mcp-server`, `api-server`, `web` as backgrounded native processes (`&` shell syntax or `nohup` per the script convention). Capture each PID via `$!` into `.mcp.pid` / `.api.pid` / `.web.pid`. Redirect stdout+stderr to `mcp-server.log` / `api-server.log` / `web.log` at repo root.
  - Per-process status banner: `[dev-up] mcp-server started (pid 12345, log mcp-server.log)`.
  - Both `api-server` and `mcp-server` are invoked with `DATABASE_URL=$(DEV_PG_CONN)` set — NOT `DB_URL` (validates US010 alignment; architecture §3 US015 + AC scenario "DATABASE_URL is the only DB env var").
  - `make dev-down`: read each PID file, signal SIGTERM, wait a short grace, SIGKILL if still alive. Remove PID file once process is confirmed gone. Then a port-kill fallback against `:8080` (api), `:8081` (mcp), `:3000` (web). Idempotent on re-run (no PID files → exit 0).
- **Env-var defaults (architecture + US015 AC scenario "env-var defaults match `startup.sh`'s current behaviour"):**
  - `API_PORT ?= 8080`, `MCP_PORT ?= 8081`, web port `3000` (Next.js default), `FRONTEND_URL ?= http://localhost:3000`, `NEXT_PUBLIC_API_BASE_URL ?= http://localhost:$(API_PORT)`. All use `?=` so operators can override from the environment.
- **`PG_CONN := → ?=` flip** (absorbed from former US011 per D-014): Makefile line ~16. Re-locate the line by content match (`PG_CONN.*postgres://.*:15432`) if line numbering has drifted. The default URL is BYTE-IDENTICAL pre-flip vs post-flip — only the operator character changes.
- **`DEV_PG_CONN` default:** `postgres://agent_board:agent_board@localhost:5432/agent_board?sslmode=disable` (architecture §3 US015 + D-013 Q1 — native Postgres on `:5432`).
- **`dev-migrate` migrations source:** every file under `services/agent-board/migrations/*.up.sql` (the same set `e2e-seed` uses, applied in lexical order). The seed source is `tests/e2e/data/seeds/*.sql` (also lexical). Both target `$(DEV_PG_CONN)` via `psql`.
- **Preflight guards** (US015 AC scenario "fail gracefully when prerequisites are missing"):
  - `command -v psql >/dev/null 2>&1 || { echo "psql not in PATH — install postgres client and retry"; exit 1; }` style for `dev-migrate` / `dev-seed`.
  - Similar checks for `go` (used by `dev-up`) and `node` / `npm` (used by `web` startup).
  - "Best-effort guard" wording is fine (AC explicit) — the bar is "new dev does not hit a 500-line stack trace."
- **e2e-family byte-identical guarantee:** before committing, run `git diff Makefile | grep -E '^[-+]' | grep -E 'e2e-'` — the only allowed changes in this category are above-recipe variable additions (e.g. the new `DEV_PG_CONN`) and the `PG_CONN := → ?=` flip, both of which preserve resolved values. Recipe bodies are byte-identical.
- **Agent-defs sweep:** `git grep -l 'startup\.sh\|shutdown\.sh' .claude/agents/` → for each match, replace references with the appropriate `make dev-*` target. Typical replacements: `./startup.sh` → `make dev-up`; `./shutdown.sh` → `make dev-down`. AFTER all edits, run `python3 scripts/sync-gemini.py` to regenerate `.gemini/agents/*.md`. Do NOT hand-edit `.gemini/agents/*.md` (CLAUDE.md rule — they're regenerated).
- **CLAUDE.md sweep:** `git grep -n 'startup\.sh\|shutdown\.sh' CLAUDE.md` — likely no hits, but verify. If hits exist, replace with `make dev-*` equivalents.
- **`docs/tech_debt.md` line 86 strike-through wording (architecture §3 US015 row):** `→ fixed in REQ006/US015 (absorbed from former US011 for line 86; PG_CONN now ?= and env-overridable)`. Re-locate by content match if line numbering has drifted (line mentions `Makefile:16 PG_CONN` / "hardcoded").

## Definition of done
- `startup.sh` and `shutdown.sh` deleted at repo root (`git ls-files startup.sh shutdown.sh` empty).
- `git grep -nE 'startup\.sh|shutdown\.sh'` shows zero hits outside REQ006-internal files (this story, sign-off log, architecture historical references). `.gemini/agents/*.md` files regenerated by `scripts/sync-gemini.py` show no `startup.sh` / `shutdown.sh` references.
- Makefile contains `PG_CONN ?=` (line ~16, default URL byte-identical) AND `DEV_PG_CONN ?= postgres://agent_board:agent_board@localhost:5432/agent_board?sslmode=disable` on its own line.
- Makefile contains four new targets: `dev-up`, `dev-down`, `dev-migrate`, `dev-seed`. Existing `e2e-*` recipe bodies are byte-identical.
- `make e2e-up && make e2e-seed && make e2e-run && make e2e-down` clean (three consecutive times — see live-e2e requirement below).
- `make dev-up` brings up three native processes with the PID + log file conventions above; `make dev-down` is clean + idempotent.
- `make dev-migrate` and `make dev-seed` apply against `$(DEV_PG_CONN)`.
- `git grep -nE 'DB_URL' Makefile` returns zero hits in any new `dev-*` recipe.
- `PG_CONN=postgres://... make e2e-seed` and `DEV_PG_CONN=postgres://... make dev-migrate` both honor the env overrides.
- `docs/tech_debt.md` line 86 strike-through applied with the exact §3 wording.
- `README.md`, `tests/e2e/README.md`, `services/agent-board/README.md` (if exists), `.claude/agents/*.md`, `CLAUDE.md` swept; no surviving `startup.sh` / `shutdown.sh` references.
- `python3 scripts/sync-gemini.py` run AFTER agent-def edits; `.gemini/agents/*.md` regenerated.
- **Review gate green:** `scripts/review/run-gate.sh cross` exits 0 with `REVIEW GATE: PASS`. (No BE-track or FE-track gate applies — no Go and no TS/TSX touched.)
- **Live e2e + 3-clean-run flake check REQUIRED** (architecture §10.1 — this story changes `make` behaviour and the regression bar is "the e2e pipeline still works"): `make e2e-up && make e2e-seed && make e2e-run && make e2e-down` clean THREE consecutive times.
- Dev set status to `in_review`; tech-lead approved.

## Notes
- **Sequencing with US010 (architecture D-013 + D-014 + R-6).** US015 SHOULD ship before or in the same merge as US010. **Rationale:** once US010 lands, the legacy `startup.sh` workflow hard-fails (because it passes `DB_URL` to mcp-server). No hard `Blocked by` link — tech-lead's call whether to pair both PRs into one merge or sequence US015 → US010 in two PRs. If US010 lands first, the breakage is loud (operator-actionable error per architecture §5.4) and the recovery is "run `make dev-up` instead of `./startup.sh`" once US015 has landed.
- **No production Go code change.** US015 is Makefile + repo-root scripts + docs + agent-defs only. Architecture D-012 (`internal/config` is the only NEW production Go file across REQ006) still holds.
- **No regression test for the `?=` flip** (architecture §3 US015 + D-014). The `?=` change is exercised implicitly by `make e2e-up` continuing to work; the standalone `scripts/review/test/test_makefile_pg_conn.sh` that original US011 had proposed is out of scope.
- **Dev type for orchestrator routing.** Spawn a `be-dev` — Makefile + bash + minimal Go familiarity. No frontend involvement.
- **Architecture §3 US015 row is the authoritative file list.** This task's `## Files touched` section mirrors it; if discrepancies arise, the architecture wins.

## Review log
