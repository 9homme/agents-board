# Tech Debt — REQ007

Non-blocking findings raised at code review or sign-off. `tech-lead-reviewer` appends one line per finding BEFORE issuing `approved`. Format and strike-through convention: see the root `docs/tech_debt.md` index.

---

## Findings

- ~~2026-06-08 — scripts/review/run-gate.sh:117 (gate_fe subshell) — the FE gate runs `npm run typecheck`, `npm run lint --max-warnings=0`, and `npm test` inside a `( cd web ... )` subshell; `fail()` increments `$FAILED` but the increment never propagates out of the subshell, so a FE typecheck/lint/test FAILURE is silently dropped and the gate still prints `REVIEW GATE: PASS` (exit 0). Only the parent-shell anti-pattern scans affect the real verdict. Discovered on US004 FE review: `npm run lint --max-warnings=0` genuinely exited 1 (`'_id' is defined but never used` in UserStoriesTab.tsx:27) yet the gate emitted PASS. This breaks the gate's core contract. Fix: capture the subshell's failure via its exit code so FE check failures count toward `$FAILED`. Route to gate-fix track — REQ007/US004/fe_user_stories_list~~ — RESOLVED 2026-06-08 by commit f380ce0: FE checks now run as `( cd web && ... ) || fail "..."` so `fail` executes in the parent shell and a genuine lint/type/test failure flips the gate to FAIL. Verified during US004 FE review pass 3 (gate now trustworthy)
- 2026-06-08 — web (Jest suite-wide) — `npm test` emits "A worker process has failed to exit gracefully and has been force exited ... tests leaking due to improper teardown" and only completes because `--forceExit` is passed. This is pre-existing and suite-wide (not introduced by US004), but `--forceExit` masks a real open-handle leak somewhere in the suite. Fix: run `npm test -- --detectOpenHandles` to locate the leaking timer/handle (likely an unaborted fetch or an unref'd timer in an older test) and fix the teardown, then drop `--forceExit`. Non-blocking — REQ007/US004/fe_user_stories_list
- 2026-06-08 — repo root: log.html, output.xml, report.html, playwright-log.txt — Robot Framework run artifacts are tracked in git on `main` and get re-modified by every `robot`/`make e2e-run`/`--dryrun` invocation, polluting `git status` and every reviewer's worktree (they showed up as spurious diffs during US002 review). They should be untracked and added to `.gitignore`. Fix: `git rm --cached log.html output.xml report.html playwright-log.txt` and add an ignore rule. Non-blocking — REQ007/US002/be_makefile_healthcheck
- 2026-06-08 — docs/requirements/REQ007_user_stories_tab_and_e2e_gate/US002_be_makefile_healthcheck.md `## Notes` — dev's pass-2 rework note claimed "Robot artifacts removed... Rebase dropped them from history entirely," but the four artifact files are in fact still tracked on `main` and re-appear as working-tree diffs after any e2e run; the claim was inaccurate (they were never committed on this branch, so the merge is clean, but the Notes wording overstated the resolution). Minor evidence-accuracy drift, no merge impact. Non-blocking — REQ007/US002/be_makefile_healthcheck
- 2026-06-08 — scripts/review/run-gate.sh (BE gate) — gosec and govulncheck are WARN-skipped ("not installed") on review hosts, so the security-scanner layer of the BE gate is silently degraded to whatever golangci-lint's gosec linter covers. govulncheck (dependency-vuln scan) has no golangci-lint equivalent and is effectively absent. Fix: pin/install gosec + govulncheck in the review environment (or vendor them via `go run`) so the BE gate's security tier is actually enforced rather than skipped. Non-blocking for US002 (Makefile-only, no Go change) — REQ007/US002/be_makefile_healthcheck
- 2026-06-08 — log.html,output.xml,report.html,playwright-log.txt (repo root) — Robot dump files are listed in .gitignore (/log.html etc.) yet are also tracked on main, so every e2e run dirties the worktree with unstaged churn on ignored-but-tracked files — `git rm --cached` them on main so they are truly untracked — REQ007/US003/be_github_actions_gate
- 2026-06-08 — .github/workflows/e2e.yml (operational) — the new e2e gate runs the FULL Robot suite via `make e2e-run`, which is currently RED (30 tests, 24 pass, 6 fail) because the REQ007 Browser/FE tests E2E-US001-001/002, E2E-US004-001/002, E2E-US005-001/002 depend on FE tabs that have not yet merged. Until US001/US004/US005 FE land, enabling branch protection on this workflow would block ALL PRs to main. Suggested fix/sequencing: do not turn on the required-status-check branch protection (the manual step in this task's `## Scope: Out`) until the REQ007 FE stories are merged and the full suite is green — REQ007/US003/be_github_actions_gate
- 2026-06-09 — git log agent/us005be (commits 3 & 4 from HEAD) — two commits prefixed `refactor:` actually changed behavior / added tests: "refactor: fix GetUserStoryTasks 404 for missing story" altered the 404 logic (a behavior change, should have been a red→green pair) and "refactor: add IT-005b ..." added a new test (not a pure refactor). TDG prefixes are valid and the red→green→refactor order holds, so non-blocking, but the labels misrepresent the change type. Fix: use `red:`/`green:` for behavior+test changes and reserve `refactor:` for no-behavior-change cleanups — REQ007/US005/be_user_story_detail
- 2026-06-09 — docs/requirements/REQ007_user_stories_tab_and_e2e_gate/US005_be_user_story_detail.md:54 (`## Notes` → "Tests added") — the Notes list claims "IT-004b" was added, but the test file contains no `IT-004b` test (it has IT-005b instead); the listed count "13 new tests" also double-counts. All spec-required IDs (UT-001..004, IT-001..007) plus the extra IT-005b are present and passing, so this is evidence-accuracy drift only. Fix: correct the Notes to match the actual test set — REQ007/US005/be_user_story_detail
- 2026-06-09 — services/agent-board/cmd/api-server/main.go (REQ Quality Gate, Mode 2) — `main.go` is listed in `## Files touched` for both US004 BE and US005 BE (route wiring added at main.go:91-93) but sits at 47.5% func coverage with NO `## Coverage exemption` section in either task. The uncovered body is the composition root only (echo bootstrap, CORS, sql.Open, signal context, DB ping, migrate.Run, repo/handler wiring, e.Start) which is conventionally non-unit-testable and is exercised by the live e2e suite; every REQ007 logic file (handlers/repos) is ≥98.8%. Not blocking quality approval (repo convention exempts the composition root; consistent with prior REQ006 main.go work), but the planner should add a standing `## Coverage exemption` for `cmd/*/main.go` to future BE tasks so the gate rule reads clean rather than relying on reviewer judgment. Fix: add the exemption boilerplate to the BE task template — REQ007/US004+US005/be tasks
- 2026-06-09 — web/package.json (npm audit, omit=dev high+) — `scripts/review/run-gate.sh fe` reports 2 advisories (1 high, 1 moderate) all in the transitive `next`/`postcss` chain (DoS via Image Optimizer remotePatterns, RSC HTTP deserialization DoS, rewrite request smuggling, CSP-nonce XSS, cache poisoning, etc.). Pre-existing and not introduced by REQ007; the gate treats `npm audit` as a non-fatal WARN so it does not flip the verdict. Fix requires `next` major bump (audit fix --force installs next@16.2.7, a breaking change) and must be scheduled as its own hardening task with a regression pass. Non-blocking — REQ007 (repo-wide dependency)
