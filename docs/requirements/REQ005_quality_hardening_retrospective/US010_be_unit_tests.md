# US010 — Backend unit & integration test specification (stub)

**No BE changes.** US010 is a FE-only story per architecture §11 and §2 US010 row. The four React-Doctor regression fixes (`MermaidDiagram` ref-attach, `useDocument` reducer refactor, `DocumentsTab` auto-select deletion, no `dangerouslySetInnerHTML`) are entirely within `web/` components and hooks. No Go files are touched; no API contracts are changed.

**BE Dev: there is nothing to implement or test for US010. This stub exists so the coverage matrix is complete.**
